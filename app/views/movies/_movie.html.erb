<div class="col-sm-12 card">
  <div class="card-body">
    <h4 class="card-title"><%= movie.title %></h4>
    <h6 class="card-subtitle mb-2 text-muted"><%= movie.release_date.try(:strftime, '%d/%m/%Y') %></h6>
    <p class="card-text"><%= movie.description %></p>
  </div>
</div>
