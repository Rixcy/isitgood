<% flash.each do |type, message| %>
  <div class="alert <%= bootstrap_class_for(type) %>" role='alert'>
    <button class="close" data-dismiss="alert">×</button>
    <span class="fa <%= bootstrap_flash_icons(type)%>" aria-hidden="true"></span>
    <span class="sr-only"><%= type.capitalize %>:</span>
    <%= message.html_safe %>
  </div>
<% end %>
